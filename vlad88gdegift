-- ФАРМ МИССИЙ С АВТОМАТИЧЕСКИМ ПОВТОРОМ
local player = game.Players.LocalPlayer
local running = true

-- Точки для телепортации в Jungle
local junglePoints = {
    CFrame.new(2434.90479, 279.143066, -1289.48108),
    CFrame.new(2734.90479, 579.143066, -1289.48108),
    CFrame.new(2834.90479, 579.143066, -1289.48108),
    CFrame.new(2438.90479, 279.143066, -1289.48108),
}

-- Точки для телепортации в Pride Rocks
local prideRocksPoints = {
    CFrame.new(1990.40833, 497.999023, -281.304352),
    CFrame.new(1795, 198, -281),
    CFrame.new(1990.41833, 199.999023, -283.304352),
    CFrame.new(1795, 198, -281),
}

-- Точки для телепортации в Swamp Hollow
local swampHollowPoints = {
    CFrame.new(711.279724, 242.321991, -2263.15088),
    CFrame.new(911.279724, 242.321991, -2263.15088),
    CFrame.new(715.279724, 243.321991, -2263.15088),
    CFrame.new(911.279724, 242.321991, -2263.15088),
}

-- Точки для Mud в каждом регионе
local jungleMudPoint = CFrame.new(2434.90479, 279.143066, -1289.48108)
local prideRocksMudPoint = CFrame.new(1795, 198, -281)
local swampHollowMudPoint = CFrame.new(711.279724, 242.321991, -2263.15088)

-- Точки перехода между регионами
local prideRocksStart = CFrame.new(1790.40833, 197.999023, -281.304352)
local swampHollowStart = CFrame.new(711.279724, 242.321991, -2263.15088)

-- Улучшенный телепорт в точку с проверками
local function teleportTo(cf)
    if not running then
        return false
    end

    local success = false
    local attempts = 0
    local maxAttempts = 3

    while not success and attempts < maxAttempts and running do
        pcall(function()
            if
                player.Character
                and player.Character:FindFirstChild('HumanoidRootPart')
            then
                player.Character.HumanoidRootPart.CFrame = cf
                success = true
            end
        end)

        if not success then
            attempts += 1
            wait(1)
        end
    end

    return success
end

-- Проверка завершения всех миссий в Jungle
local function allJungleMissionsCompleted()
    local missions =
        game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle
    return missions.TimePlayed.Value
        and missions.Sniff.Value
        and missions.EatFoodDrinkWater.Value
        and missions.DistanceTravelled.Value
        and missions.ConcealScent.Value
        and missions.AttackOrHealCreatureOrNPC.Value
end

-- Проверка завершения всех миссий в Pride Rocks
local function allPrideRocksMissionsCompleted()
    local missions =
        game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks']
    return missions.TimePlayed.Value
        and missions.Sniff.Value
        and missions.EatFoodDrinkWater.Value
        and missions.DistanceTravelled.Value
        and missions.ConcealScent.Value
        and missions.AttackOrHealCreatureOrNPC.Value
end

-- Проверка завершения всех миссий в Swamp Hollow
local function allSwampHollowMissionsCompleted()
    local missions =
        game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow']
    return missions.TimePlayed.Value
        and missions.Sniff.Value
        and missions.EatFoodDrinkWater.Value
        and missions.DistanceTravelled.Value
        and missions.ConcealScent.Value
        and missions.AttackOrHealCreatureOrNPC.Value
end

-- Проверка, есть ли активные миссии в Jungle
local function hasActiveJungleMissions()
    local missions =
        game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle
    return not missions.TimePlayed.Value
        or not missions.Sniff.Value
        or not missions.EatFoodDrinkWater.Value
        or not missions.DistanceTravelled.Value
        or not missions.ConcealScent.Value
        or not missions.AttackOrHealCreatureOrNPC.Value
end

-- Проверка, есть ли активные миссии в Pride Rocks
local function hasActivePrideRocksMissions()
    local missions =
        game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks']
    return not missions.TimePlayed.Value
        or not missions.Sniff.Value
        or not missions.EatFoodDrinkWater.Value
        or not missions.DistanceTravelled.Value
        or not missions.ConcealScent.Value
        or not missions.AttackOrHealCreatureOrNPC.Value
end

-- Проверка, есть ли активные миссии в Swamp Hollow
local function hasActiveSwampHollowMissions()
    local missions =
        game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow']
    return not missions.TimePlayed.Value
        or not missions.Sniff.Value
        or not missions.EatFoodDrinkWater.Value
        or not missions.DistanceTravelled.Value
        or not missions.ConcealScent.Value
        or not missions.AttackOrHealCreatureOrNPC.Value
end

-- Ожидание сброса миссий
local function waitForMissionsReset()
    print("=== ВСЕ МИССИИ ВЫПОЛНЕНЫ ===")
    print("Ожидаем сброса миссий (5 минут)...")
    
    local startTime = tick()
    local checkInterval = 30 -- Проверяем каждые 30 секунд
    local lastCheckTime = 0
    
    while running do
        local currentTime = tick()
        
        -- Проверяем миссии каждые 30 секунд
        if currentTime - lastCheckTime >= checkInterval then
            lastCheckTime = currentTime
            local elapsedMinutes = math.floor((currentTime - startTime) / 60)
            
            -- Проверяем, есть ли активные миссии
            local jungleActive = hasActiveJungleMissions()
            local prideRocksActive = hasActivePrideRocksMissions()
            local swampHollowActive = hasActiveSwampHollowMissions()
            
            if jungleActive or prideRocksActive or swampHollowActive then
                print("✅ Миссии сбросились! Запускаем фарм заново...")
                return true
            else
                print("⏳ Ожидаем сброса... Прошло " .. elapsedMinutes .. " минут")
            end
        end
        
        wait(1)
    end
    
    return false
end

-- Функция для получения ВСЕХ Mud объектов и использования последнего
local function getAllMudObjects()
    local mudFolder = workspace:WaitForChild("Interactions"):WaitForChild("Mud")
    local allMudObjects = {}
    
    -- Собираем все Mud объекты
    for _, mudModel in pairs(mudFolder:GetChildren()) do
        if mudModel:IsA("Model") then
            -- Ищем часть "Mud" внутри модели
            local mudPart = mudModel:FindFirstChild("Mud")
            if mudPart then
                table.insert(allMudObjects, mudPart)
            end
        end
    end
    
    print("Найдено Mud объектов: " .. #allMudObjects)
    
    -- Возвращаем последний объект (или nil если нет объектов)
    if #allMudObjects > 0 then
        return allMudObjects[#allMudObjects]
    else
        return nil
    end
end

-- ФУНКЦИЯ MUD КОТОРАЯ РАБОТАЕТ ПОКА МИССИЯ НЕ ВЫПОЛНИТСЯ
local function doMudRoll(region)
    print("=== НАЧИНАЕМ MUD ROLL ДЛЯ " .. region:upper() .. " ===")
    
    local mudMission
    if region == 'Jungle' then
        mudMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.DistanceTravelled
    elseif region == 'Pride Rocks' then
        mudMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].DistanceTravelled
    else -- Swamp Hollow
        mudMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].DistanceTravelled
    end

    if mudMission.Value then
        print("Mud Roll уже выполнен!")
        return true
    end

    -- Телепортируемся к точке Mud для региона
    local mudPoint
    if region == 'Jungle' then
        mudPoint = jungleMudPoint
    elseif region == 'Pride Rocks' then
        mudPoint = prideRocksMudPoint
    else -- Swamp Hollow
        mudPoint = swampHollowMudPoint
    end
    
    print("Телепортируемся к Mud точке...")
    teleportTo(mudPoint)
    wait(2)

    -- ИСПОЛЬЗУЕМ MUD ПОКА МИССИЯ НЕ ВЫПОЛНИТСЯ
    local attempt = 0
    while running and not mudMission.Value do
        attempt += 1
        
        pcall(function()
            -- Используем последний Mud объект
            local lastMud = getAllMudObjects()
            if lastMud then
                local args = { lastMud }
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Mud"):FireServer(unpack(args))
                print("Попытка " .. attempt .. ": Использован Mud (объект: " .. lastMud.Name .. ")")
            else
                print("❌ Mud объекты не найдены! Пробуем снова...")
                -- Если Mud не найден, снова телепортируемся к точке
                teleportTo(mudPoint)
                wait(1)
            end
        end)
        
        -- Ждем перед следующей попыткой
        wait(1)
        
        -- Проверяем каждые 10 попыток
        if attempt % 10 == 0 then
            print("Проверяем статус миссии... Попыток: " .. attempt)
        end
    end

    if mudMission.Value then
        print("✅ Mud Roll ВЫПОЛНЕН! Всего попыток: " .. attempt)
        return true
    else
        print("❌ Mud Roll не выполнен (остановлен пользователем)")
        return false
    end
end

-- DistanceTravelled для Jungle
local function doJungleDistanceTravelled()
    local distanceMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.DistanceTravelled

    -- Сначала Mud Roll (будет работать пока не выполнится)
    print("=== ВЫПОЛНЯЕМ MUD ROLL ===")
    doMudRoll('Jungle')

    -- Потом дистанция (только если Mud выполнен)
    if distanceMission.Value == false then
        print("=== НАЧИНАЕМ DISTANCE TRAVELLED ===")
        while running and not allJungleMissionsCompleted() and distanceMission.Value == false do
            for i = 1, #junglePoints do
                if not running or allJungleMissionsCompleted() or distanceMission.Value then
                    break
                end
                print("Телепорт к Jungle точке " .. i)
                teleportTo(junglePoints[i])
                wait(2)
            end
        end
    end
    
    if distanceMission.Value then
        print("✅ Jungle DistanceTravelled ВЫПОЛНЕН!")
    end
end

-- DistanceTravelled для Pride Rocks
local function doPrideRocksDistanceTravelled()
    local distanceMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].DistanceTravelled

    -- Сначала Mud Roll (будет работать пока не выполнится)
    print("=== ВЫПОЛНЯЕМ MUD ROLL ===")
    doMudRoll('Pride Rocks')

    -- Потом дистанция (только если Mud выполнен)
    if distanceMission.Value == false then
        print("=== НАЧИНАЕМ DISTANCE TRAVELLED ===")
        while running and not allPrideRocksMissionsCompleted() and distanceMission.Value == false do
            for i = 1, #prideRocksPoints do
                if not running or allPrideRocksMissionsCompleted() or distanceMission.Value then
                    break
                end
                print("Телепорт к Pride Rocks точке " .. i)
                teleportTo(prideRocksPoints[i])
                wait(2)
            end
        end
    end
    
    if distanceMission.Value then
        print("✅ Pride Rocks DistanceTravelled ВЫПОЛНЕН!")
    end
end

-- DistanceTravelled для Swamp Hollow
local function doSwampHollowDistanceTravelled()
    local distanceMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].DistanceTravelled

    -- Сначала Mud Roll (будет работать пока не выполнится)
    print("=== ВЫПОЛНЯЕМ MUD ROLL ===")
    doMudRoll('Swamp Hollow')

    -- Потом дистанция (только если Mud выполнен)
    if distanceMission.Value == false then
        print("=== НАЧИНАЕМ DISTANCE TRAVELLED ===")
        while running and not allSwampHollowMissionsCompleted() and distanceMission.Value == false do
            for i = 1, #swampHollowPoints do
                if not running or allSwampHollowMissionsCompleted() or distanceMission.Value then
                    break
                end
                print("Телепорт к Swamp Hollow точке " .. i)
                teleportTo(swampHollowPoints[i])
                wait(2)
            end
        end
    end
    
    if distanceMission.Value then
        print("✅ Swamp Hollow DistanceTravelled ВЫПОЛНЕН!")
    end
end

-- Атака NPC
local function attackNPC(region)
    local lastAttackTime = 0
    local attackCooldown = 5
    
    while running do
        local attackMission
        if region == 'Jungle' then
            attackMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.AttackOrHealCreatureOrNPC
        elseif region == 'Pride Rocks' then
            attackMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].AttackOrHealCreatureOrNPC
        else -- Swamp Hollow
            attackMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].AttackOrHealCreatureOrNPC
        end

        if attackMission.Value then
            print("Атака NPC уже выполнена")
            break
        end

        local currentTime = tick()
        if currentTime - lastAttackTime >= attackCooldown then
            pcall(function()
                local args = {
                    {
                        workspace:WaitForChild("Interactions"):WaitForChild("NPCs"):WaitForChild("Roots"):WaitForChild("RootPart")
                    }
                }
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("NPCDamageRemote"):FireServer(unpack(args))
                print("Атаковали NPC")
                lastAttackTime = currentTime
            end)
        end

        wait(1)
    end
end

-- Еда и вода
local function eatAndDrink(region)
    while running do
        local eatMission
        if region == 'Jungle' then
            eatMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.EatFoodDrinkWater
        elseif region == 'Pride Rocks' then
            eatMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].EatFoodDrinkWater
        else -- Swamp Hollow
            eatMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].EatFoodDrinkWater
        end

        if eatMission.Value then
            break
        end

        -- Еда 25 раз
        print("Начинаем есть 25 раз...")
        pcall(function()
            local foodFolder = workspace:WaitForChild("Interactions"):WaitForChild("Food")
            local foods = foodFolder:GetChildren()
            
            for i = 1, 25 do
                if not running or eatMission.Value then
                    break
                end
                
                local food = foods[i] or foods[1]
                local args = { food }
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Food"):FireServer(unpack(args))
                print("Съели еду " .. i .. "/25")
                wait(0.3)
            end
        end)

        -- Вода
        pcall(function()
            local lake = workspace:WaitForChild("Interactions"):WaitForChild("Lakes"):FindFirstChild("Lake")
            if lake then
                local args = { lake }
                game:GetService('ReplicatedStorage'):WaitForChild('Remotes'):WaitForChild('DrinkRemote'):FireServer(unpack(args))
                print("Выпили воду")
            end
        end)

        wait(2)
    end
end

-- Sniff миссия
local function doSniff(region)
    while running do
        local sniffMission
        if region == 'Jungle' then
            sniffMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.Sniff
        elseif region == 'Pride Rocks' then
            sniffMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].Sniff
        else -- Swamp Hollow
            sniffMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].Sniff
        end

        if sniffMission.Value then
            break
        end

        pcall(function()
            local args = { 1 }
            game:GetService('ReplicatedStorage'):WaitForChild('Remotes'):WaitForChild('SetMissionRemote'):FireServer(unpack(args))
            print("Снифф выполнен")
        end)
        wait(2)
    end
end

-- ConcealScent миссия (ТОЖЕ РАБОТАЕТ ПОКА НЕ ВЫПОЛНИТСЯ)
local function doConcealScent(region)
    while running do
        local concealMission
        if region == 'Jungle' then
            concealMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.ConcealScent
        elseif region == 'Pride Rocks' then
            concealMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].ConcealScent
        else -- Swamp Hollow
            concealMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].ConcealScent
        end

        if concealMission.Value then
            print("ConcealScent выполнен")
            break
        end

        pcall(function()
            -- Для ConcealScent тоже используем последний Mud объект
            local lastMud = getAllMudObjects()
            if lastMud then
                local args = { lastMud }
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Mud"):FireServer(unpack(args))
                print("ConcealScent Mud (объект: " .. lastMud.Name .. ")")
            else
                print("❌ Mud объекты не найдены для ConcealScent!")
            end
        end)
        wait(2)
    end
end

-- TimePlayed
local function waitForTimePlayed(region)
    while running do
        local timeMission
        if region == 'Jungle' then
            timeMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions.Jungle.TimePlayed
        elseif region == 'Pride Rocks' then
            timeMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Pride Rocks'].TimePlayed
        else -- Swamp Hollow
            timeMission = game:GetService('Players')[player.Name].PlayerGui.Data.Missions.RegionMissions['Swamp Hollow'].TimePlayed
        end

        if timeMission.Value then
            break
        end
        wait(5)
    end
end

-- Фарм региона
local function farmRegion(region)
    local points
    if region == 'Jungle' then
        points = junglePoints
    elseif region == 'Pride Rocks' then
        points = prideRocksPoints
    else -- Swamp Hollow
        points = swampHollowPoints
    end

    teleportTo(points[1])
    wait(1)

    print("=== НАЧИНАЕМ " .. region:upper() .. " ===")
    
    if region == 'Jungle' then
        doJungleDistanceTravelled()
    elseif region == 'Pride Rocks' then
        doPrideRocksDistanceTravelled()
    else -- Swamp Hollow
        doSwampHollowDistanceTravelled()
    end

    -- Параллельные миссии
    coroutine.wrap(function() attackNPC(region) end)()
    coroutine.wrap(function() eatAndDrink(region) end)()
    coroutine.wrap(function() doSniff(region) end)()
    coroutine.wrap(function() doConcealScent(region) end)()
    coroutine.wrap(function() waitForTimePlayed(region) end)()

    -- Ждем завершения
    local checkFunction
    if region == 'Jungle' then
        checkFunction = allJungleMissionsCompleted
    elseif region == 'Pride Rocks' then
        checkFunction = allPrideRocksMissionsCompleted
    else -- Swamp Hollow
        checkFunction = allSwampHollowMissionsCompleted
    end
    
    while running and not checkFunction() do
        wait(1)
    end

    return checkFunction()
end

-- Основная функция с бесконечным циклом
local function startFarming()
    print("🚀 ЗАПУСК ФАРМА МИССИЙ...")
    
    -- Бесконечный цикл фарма
    while running do
        -- Jungle
        local jungleCompleted = farmRegion('Jungle')

        -- Pride Rocks
        if jungleCompleted and running then
            print("🔄 ПЕРЕХОД В PRIDE ROCKS")
            if teleportTo(prideRocksStart) then
                wait(2)
                local prideRocksCompleted = farmRegion('Pride Rocks')
                
                -- Swamp Hollow
                if prideRocksCompleted and running then
                    print("🔄 ПЕРЕХОД В SWAMP HOLLOW")
                    if teleportTo(swampHollowStart) then
                        wait(2)
                        local swampHollowCompleted = farmRegion('Swamp Hollow')
                        
                        -- Если все три региона завершены, ждем сброса миссий
                        if swampHollowCompleted and running then
                            print("🎉 ВСЕ МИССИИ ВЫПОЛНЕНЫ!")
                            wait(3)
                            
                            -- Ждем сброса миссий
                            if not waitForMissionsReset() then
                                break -- Если вышли из ожидания
                            end
                            
                            -- Запускаем фарм заново
                            print("=== ЗАПУСКАЕМ ФАРМ ЗАНОВО ===")
                        end
                    end
                end
            end
        else
            break
        end
    end

    print("⏹️ ФАРМ ОСТАНОВЛЕН")
end

-- Запускаем фарм сразу
startFarming()

-- Функция для остановки (можно вызвать в консоли)
function stopFarming()
    running = false
    print("Фарм остановлен")
end

print('ФАРМ МИССИЙ С 3 ЛОКАЦИЯМИ ЗАПУЩЕН! Для остановки введите: stopFarming()')
